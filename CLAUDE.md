@AGENTS.md
@core/core.ja.md

Claude Code specifics:

- Re-verify a primary source with a real fetch (WebFetch/curl); a summary from memory is not a citable quote.
- Load the `writing-compose` or `writing-review` skill explicitly for a README, design doc, or report; do not assume it fires from the request's wording alone.
